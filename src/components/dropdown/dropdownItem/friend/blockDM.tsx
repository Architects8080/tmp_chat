import React from "react";
import DefaultDropdownItem from "../../itemTemplate/default/item";

function BlockDMItem() {
  return (
    <DefaultDropdownItem
      title="DM 차단하기 (Block)"
      color="red"
      callback={() => {}}
    />
  );
}

export default BlockDMItem;
